server:
    port: 8001
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/    
Feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"